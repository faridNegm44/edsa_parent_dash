{{-- $(document).on('click', '#example2 #view', function(e){
    e.preventDefault();
    let act = $(this).attr('act');

    alert(act);

    $.ajax({
        type: 'post',
        headers: {'XSRF-TOKEN': $('meta[name="_token"]').attr('content')},
        url: act,
        success: function(){

        }
    });
}); --}}
